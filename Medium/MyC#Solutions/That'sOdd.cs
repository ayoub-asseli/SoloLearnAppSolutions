using System;

class Program {
    static void Main(string[] args) {
    	int n = Int32.Parse(Console.ReadLine());
    	int s = 0;
    	for (int i = O; i < n; i++){
    		int nb = Int32.Parse(Console.ReadLine());
    		if (nb % 2 == 0) 
    			s += nb
    	}
    	Console.WriteLine(s);
    }
}